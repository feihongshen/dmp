select update_time from `express_set_branch` where 1 = 2;	-- 没有报错
select update_time from `express_set_customer_info` where 1 = 2;	-- 没有报错
select update_time from `express_set_user` where 1 = 2;	-- 没有报错
select update_time from `express_ops_order_intowarhouse` where 1 = 2;	-- 没有报错
select update_time from `express_set_reason` where 1 = 2;	-- 没有报错
select update_time from `express_ops_orderback_record` where 1 = 2;	-- 没有报错
select update_time from `fn_customer_bill` where 1 = 2;	-- 没有报错
select update_time from `express_set_role_new` where 1 = 2;	-- 没有报错
select update_time from `ops_ypdjhandlerecord` where 1 = 2;	-- 没有报错
